export type Meal = {
    id: string
    title: string,
    category: string,
    instructions: string
    thumbnail: string
    tags: string
}

export type JSONMeal = {
    idMeal: string
    strMeal: string,
    strCategory: string,
    strInstructions: string
    strMealThumb: string
    strTags: string
}